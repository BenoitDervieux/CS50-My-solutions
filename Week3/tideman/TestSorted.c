#include <stdio.h>

int main(void)
{
    int premiere_valeur;
    int deuxieme_valeur;
    int valeur_total;
    int minimum_valeur;
    int valeur_temporaire;
    int valeurs_listees[valeur_total];
    
    printf("Entrez un nombre de chiffres à trier: ");
    scanf("%d", &valeur_total);
    printf("Entrez les chiffres à tirer: \n");
    
    for (premiere_valeur = 0; premiere_valeur < valeur_total; premiere_valeur++) 
    {
        scanf("%d", &valeurs_listees[premiere_valeur]);
    }
    
    for (premiere_valeur = 0; premiere_valeur < valeur_total - 1; premiere_valeur++)
    {
        minimum_valeur = premiere_valeur;
    }
    for (deuxieme_valeur = 0; deuxieme_valeur < valeur_total; deuxieme_valeur++) {
        if(valeursvaleurs_listees[deuxieme_valeur] < valeursvaleurs_listees[deuxieme_valeur] ) 
        {
            minimum_valeur
        } 
    }
    
}